export interface LookupAllPlayers {
  player: [
    {
      idPlayer: string,
      strPlayer: string,
      strSigning: string,
      strPosition: string,
      strCutout: string,
      dateBorn: string,
    }
  ];
}
